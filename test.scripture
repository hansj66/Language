void TestFunction(number arg1, number arg2)
{
    print arg1+arg2;
}

int main(number a, number b)
{
    number n1;
    number n2;
    n1 = 3;
    n2 = 5;
    while (n2 < 100)
    {
        TestFunction(n1,n2);
        n2 = n2+1;
     }
}
